// Narrow store for vault ack counters; legacy implementation lives in messages.js.

import { getVaultAckCounter, recordVaultAckCounter } from '../messages.js';

export { getVaultAckCounter, recordVaultAckCounter };
